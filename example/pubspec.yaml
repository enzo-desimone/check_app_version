name: example
description: Flutter example for check_app_version
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  check_app_version:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
